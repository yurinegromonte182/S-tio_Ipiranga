name: Teste Simples Telegram

on: workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
    
    steps:
      - name: Enviar mensagem de teste
        run: |
          MESSAGE="🧪 *Teste do workflow*%0A"
          MESSAGE="${MESSAGE}✅ Sistema operacional%0A"
          MESSAGE="${MESSAGE}📅 $(date)%0A"
          MESSAGE="${MESSAGE}%0A🌿 Sítio Ipiranga PWA"
          
          curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
            -d chat_id="$TELEGRAM_CHAT_ID" \
            -d text="$MESSAGE" \
            -d parse_mode="Markdown"
          
          echo "✅ Mensagem de teste enviada!"
