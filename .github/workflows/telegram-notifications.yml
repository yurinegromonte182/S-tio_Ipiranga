name: Notificações Telegram - Supabase

on:
  schedule:
    # 06:00 BR = 09:00 UTC
    - cron: '0 9 * * *'
    # 12:00 BR = 15:00 UTC  
    - cron: '0 15 * * *'
    # 18:00 BR = 21:00 UTC
    - cron: '0 21 * * *'
  workflow_dispatch:

jobs:
  notify-telegram:
    runs-on: ubuntu-latest
    env:
      SUPABASE_URL: https://erbefbnjxgpetbetlzya.supabase.co
      SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVyYmVmYm5qeGdwZXRiZXRsenlhIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2MTAzMjg4NywiZXhwIjoyMDc2NjA4ODg3fQ.JWfc7IshcM5nETqAyVIe93H0KjGxf_XFcfaQ-S9L8Dw
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Setup environment
        run: |
          # Usar sudo para instalar pacotes
          sudo apt-get update
          sudo apt-get install -y curl jq bc
          
          # Configurar fuso horário
          export TZ='America/Sao_Paulo'
          
          # Data atual
          HOJE=$(date +%Y-%m-%d)
          DATA_EXIBICAO=$(date +%d/%m/%Y)
          HORA_EXIBICAO=$(date +%H:%M)
          HORA_NUM=$(date +%H)
          
          echo "HOJE=$HOJE" >> $GITHUB_ENV
          echo "DATA_EXIBICAO=$DATA_EXIBICAO" >> $GITHUB_ENV
          echo "HORA_EXIBICAO=$HORA_EXIBICAO" >> $GITHUB_ENV
          echo "HORA_NUM=$HORA_NUM" >> $GITHUB_ENV
      
      - name: Buscar dados do Supabase
        id: get-data
        run: |
          # Função para calcular dias
          dias_desde() {
            if [ -z "$1" ] || [ "$1" = "null" ] || [ "$1" = "" ]; then
              echo "999"
            else
              data_epoch=$(date -d "$1" +%s 2>/dev/null || echo "0")
              hoje_epoch=$(date -d "$HOJE" +%s)
              if [ "$data_epoch" = "0" ]; then
                echo "999"
              else
                echo $(( (hoje_epoch - data_epoch) / 86400 ))
              fi
            fi
          }
          
          # Função para normalizar texto
          normalizar_texto() {
            echo "$1" | tr '[:upper:]' '[:lower:]' | sed 's/^ *//;s/ *$//'
          }
          
          echo "🔄 Tentando buscar dados do Supabase..."
          
          # Tentar buscar da função RPC
          RESPONSE=$(curl -s -X POST \
            "$SUPABASE_URL/rest/v1/rpc/get_plantas_para_telegram" \
            -H "apikey: $SUPABASE_ANON_KEY" \
            -H "Authorization: Bearer $SUPABASE_ANON_KEY" \
            -H "Content-Type: application/json" \
            -d '{}' 2>/dev/null || echo "[]")
          
          # Verificar se a resposta é válida
          if echo "$RESPONSE" | jq -e . >/dev/null 2>&1; then
            TOTAL=$(echo "$RESPONSE" | jq '. | length')
            echo "✅ $TOTAL plantas encontradas no Supabase"
            echo "TOTAL=$TOTAL" >> $GITHUB_ENV
            echo "$RESPONSE" > plantas.json
            
            # Salvar também em formato processável
            echo "$RESPONSE" | jq -c '.[]' > plantas_array.json
          else
            echo "⚠️ Erro ao buscar do Supabase, usando backup local..."
            
            # Verificar se existe arquivo local
            if [ -f "dados.geojson" ]; then
              TOTAL=$(jq '.features | length' dados.geojson 2>/dev/null || echo "0")
              echo "TOTAL=$TOTAL" >> $GITHUB_ENV
              echo "✅ $TOTAL plantas no backup local"
              
              # Converter para formato similar
              jq '[.features[] | {
                id: (.properties.id // .properties.fid // "S/N"),
                nome: (.properties.nome_popular // "Planta"),
                ultRega: .properties.ultima_rega,
                ultPoda: .properties.ultima_poda,
                ultColh: .properties.ultima_colheita,
                dataPlantio: .properties.data_plantio
              }]' dados.geojson > plantas.json
            else
              echo "TOTAL=0" >> $GITHUB_ENV
              echo '[]' > plantas.json
            fi
          fi
          
          echo "total_plantas=$TOTAL" >> $GITHUB_OUTPUT
      
      - name: Processar notificação
        id: process-notify
        run: |
          TOTAL=${{ env.TOTAL }}
          
          if [ "$TOTAL" -eq "0" ] || [ ! -s "plantas.json" ]; then
            echo "❌ Sem dados para processar"
            echo "MESSAGE=⚠️ *Sítio Ipiranga - Sem dados*%0ANenhuma planta encontrada no sistema." >> $GITHUB_ENV
            exit 0
          fi
          
          # Saudação
          if [ "$HORA_NUM" -lt 12 ]; then
            SAUDACAO="☀️ Bom dia"
          elif [ "$HORA_NUM" -lt 18 ]; then
            SAUDACAO="⛅ Boa tarde"
          else
            SAUDACAO="🌙 Boa noite"
          fi
          
          # Iniciar mensagem
          MESSAGE="${SAUDACAO}!%0A"
          MESSAGE="${MESSAGE}🌿 *Sítio Ipiranga*%0A"
          MESSAGE="${MESSAGE}📅 ${{ env.DATA_EXIBICAO }} às ${{ env.HORA_EXIBICAO }}%0A"
          MESSAGE="${MESSAGE}🌳 *${TOTAL} plantas* cadastradas%0A%0A"
          
          # Arrays para ações
          REGA_URG=""
          REGA_NORM=""
          PODA_PEND=""
          COLHEITA_PEND=""
          TOTAL_ACOES=0
          
          # Processar cada planta
          while IFS= read -r planta; do
            ID=$(echo "$planta" | jq -r '.id // .planta_id // "S/N"')
            NOME=$(echo "$planta" | jq -r '.nome // .nome_popular // "Planta"')
            ULT_REGA=$(echo "$planta" | jq -r '.ultRega // .ultima_rega // "null"')
            ULT_PODA=$(echo "$planta" | jq -r '.ultPoda // .ultima_poda // "null"')
            ULT_COLH=$(echo "$planta" | jq -r '.ultColh // .ultima_colheita // "null"')
            
            # Normalizar nome
            NOME_NORM=$(echo "$NOME" | tr '[:upper:]' '[:lower:]')
            
            # Determinar intervalos
            if echo "$NOME_NORM" | grep -q "mangabeira"; then
              REGA_INT=7; PODA_INT=180; COLHEITA_INT=90
            elif echo "$NOME_NORM" | grep -q "caju"; then
              REGA_INT=10; PODA_INT=365; COLHEITA_INT=60
            elif echo "$NOME_NORM" | grep -q "coq"; then
              REGA_INT=5; PODA_INT=730; COLHEITA_INT=30
            elif echo "$NOME_NORM" | grep -q "cacau"; then
              REGA_INT=3; PODA_INT=180; COLHEITA_INT=120
            else
              REGA_INT=7; PODA_INT=180; COLHEITA_INT=90
            fi
            
            # Verificar rega
            DIAS_R=$(dias_desde "$ULT_REGA")
            if [ "$DIAS_R" -ge $((REGA_INT + 2)) ]; then
              REGA_URG="${REGA_URG}• 🚨 ${NOME} #${ID} - ${DIAS_R} dias%0A"
              TOTAL_ACOES=$((TOTAL_ACOES + 1))
            elif [ "$DIAS_R" -ge "$REGA_INT" ]; then
              REGA_NORM="${REGA_NORM}• 💧 ${NOME} #${ID} - ${DIAS_R} dias%0A"
              TOTAL_ACOES=$((TOTAL_ACOES + 1))
            fi
            
            # Verificar poda
            DIAS_P=$(dias_desde "$ULT_PODA")
            if [ "$DIAS_P" -ge "$PODA_INT" ]; then
              PODA_PEND="${PODA_PEND}• ✂️ ${NOME} #${ID} - ${DIAS_P} dias%0A"
              TOTAL_ACOES=$((TOTAL_ACOES + 1))
            fi
            
            # Verificar colheita
            DIAS_C=$(dias_desde "$ULT_COLH")
            if [ "$DIAS_C" -ge "$COLHEITA_INT" ]; then
              COLHEITA_PEND="${COLHEITA_PEND}• 🍎 ${NOME} #${ID} - ${DIAS_C} dias%0A"
              TOTAL_ACOES=$((TOTAL_ACOES + 1))
            fi
            
          done < <(jq -c '.[]' plantas.json)
          
          # Montar mensagem final
          if [ "$TOTAL_ACOES" -eq 0 ]; then
            MESSAGE="${MESSAGE}✅ *Tudo em dia!*%0ANenhuma ação pendente.%0A"
          else
            MESSAGE="${MESSAGE}📋 *${TOTAL_ACOES} ação(ões) pendente(s):*%0A%0A"
            [ -n "$REGA_URG" ] && MESSAGE="${MESSAGE}🚨 *URGENTE:*%0A${REGA_URG}%0A"
            [ -n "$REGA_NORM" ] && MESSAGE="${MESSAGE}💧 *Rega:*%0A${REGA_NORM}%0A"
            [ -n "$PODA_PEND" ] && MESSAGE="${MESSAGE}✂️ *Poda:*%0A${PODA_PEND}%0A"
            [ -n "$COLHEITA_PEND" ] && MESSAGE="${MESSAGE}🍎 *Colheita:*%0A${COLHEITA_PEND}%0A"
          fi
          
          MESSAGE="${MESSAGE}%0A📱 *Acesse o app:*%0A"
          MESSAGE="${MESSAGE}https://yurimegromonte182.github.io/S-tio_Jintanga/"
          
          echo "MESSAGE=$MESSAGE" >> $GITHUB_ENV
          echo "total_acoes=$TOTAL_ACOES" >> $GITHUB_OUTPUT
      
      - name: Enviar para Telegram
        run: |
          curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
            -d chat_id="$TELEGRAM_CHAT_ID" \
            -d text="$MESSAGE" \
            -d parse_mode="Markdown"
          
          echo "✅ Notificação enviada!"
      
      - name: Estatísticas diárias (06h)
        if: github.event.schedule == '0 9 * * *'
        run: |
          if [ "${{ env.TOTAL }}" -gt "0" ]; then
            # Contar por tipo
            COQUEIROS=0
            CAJUEIROS=0
            CACAU=0
            MANGABEIRA=0
            
            while IFS= read -r planta; do
              NOME=$(echo "$planta" | jq -r '.nome // .nome_popular // ""' | tr '[:upper:]' '[:lower:]')
              
              if echo "$NOME" | grep -q "coq"; then
                COQUEIROS=$((COQUEIROS + 1))
              elif echo "$NOME" | grep -q "caju"; then
                CAJUEIROS=$((CAJUEIROS + 1))
              elif echo "$NOME" | grep -q "cacau"; then
                CACAU=$((CACAU + 1))
              elif echo "$NOME" | grep -q "mangabeira"; then
                MANGABEIRA=$((MANGABEIRA + 1))
              fi
            done < <(jq -c '.[]' plantas.json)
            
            STATS="📊 *Resumo do Sítio:*%0A"
            STATS="${STATS}🌳 Total: ${{ env.TOTAL }} plantas%0A"
            [ "$COQUEIROS" -gt 0 ] && STATS="${STATS}🥥 Coqueiros: ${COQUEIROS}%0A"
            [ "$CAJUEIROS" -gt 0 ] && STATS="${STATS}🌰 Cajueiros: ${CAJUEIROS}%0A"
            [ "$CACAU" -gt 0 ] && STATS="${STATS}🍫 Cacau: ${CACAU}%0A"
            [ "$MANGABEIRA" -gt 0 ] && STATS="${STATS}🥭 Mangabeira: ${MANGABEIRA}%0A"
            
            curl -s -X POST "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
              -d chat_id="$TELEGRAM_CHAT_ID" \
              -d text="$STATS" \
              -d parse_mode="Markdown"
          fi
