name: Notificações Automáticas de Frutíferas

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  notificacoes:
    runs-on: ubuntu-latest

    steps:
    - name: 🔧 Preparar ambiente
      run: |
        sudo apt-get update
        sudo apt-get install -y jq curl

    - name: 🔑 Carregar variáveis dos secrets
      run: |
        echo "SUPABASE_URL=${{ secrets.SUPABASE_URL }}" >> $GITHUB_ENV
        echo "SUPABASE_SERVICE_KEY=${{ secrets.SUPABASE_SERVICE_KEY }}" >> $GITHUB_ENV
        echo "BOT_TOKEN=${{ secrets.BOT_TOKEN }}" >> $GITHUB_ENV
        echo "CHAT_ID=${{ secrets.CHAT_ID }}" >> $GITHUB_ENV

    - name: 📥 Buscar dados do Supabase e enviar notificações
      run: |
        SUPABASE_URL="${SUPABASE_URL}"
        SUPABASE_SERVICE_KEY="${SUPABASE_SERVICE_KEY}"
        BOT_TOKEN="${BOT_TOKEN}"
        CHAT_ID="${CHAT_ID}"

        ENDPOINT="${SUPABASE_URL}/rest/v1/frutiferas?select=*"

        echo "📡 Baixando dados do Supabase..."
        dados=$(curl -s "$ENDPOINT" \
          -H "apikey: $SUPABASE_SERVICE_KEY" \
          -H "Authorization: Bearer $SUPABASE_SERVICE_KEY")

        total=$(echo "$dados" | jq 'length')
        echo "📌 Registros recebidos: $total"

        echo "$dados" | jq -c '.[]' | while read item; do
            id=$(echo "$item" | jq -r '.id')
            nome=$(echo "$item" | jq -r '.nome_popular')
            ultima_rega=$(echo "$item" | jq -r '.data_irrigacao_ordinaria')
            ultima_poda=$(echo "$item" | jq -r '.data_poda_formacao_1')
            inicio_colheita=$(echo "$item" | jq -r '.inicio_colheita')

            mensagem=$(cat <<EOF
🌱 *Planta:* ${nome}
🔢 *ID:* ${id}
💧 Última rega: ${ultima_rega}
✂️ Última poda: ${ultima_poda}
🍊 Início da colheita: ${inicio_colheita}
EOF
)

            curl -s -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
              -d chat_id="${CHAT_ID}" \
              -d text="${mensagem}" \
              -d parse_mode="Markdown"

            echo "📨 Notificação enviada: ${nome} (ID ${id})"
            sleep 1
        done

        echo "✅ Finalizado com sucesso!"
