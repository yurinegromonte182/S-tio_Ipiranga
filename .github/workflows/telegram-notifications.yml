name: Notificações Telegram - Sincronização Automática

on:
  push:
    branches:
      - main
    paths:
      - 'dados.geojson'
  schedule:
    # 06:00 BR = 09:00 UTC
    - cron: '0 9 * * *'
    # 12:00 BR = 15:00 UTC
    - cron: '0 15 * * *'
    # 18:00 BR = 21:00 UTC
    - cron: '0 21 * * *'
  workflow_dispatch:

jobs:
  notify-telegram:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Processar e notificar
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          # Configurar fuso horário
          export TZ='America/Sao_Paulo'
          
          # Data atual
          HOJE=$(date +%Y-%m-%d)
          DATA_EXIBICAO=$(date +%d/%m/%Y)
          HORA_EXIBICAO=$(date +%H:%M)
          HORA_NUM=$(date +%H)
          
          # Função para calcular dias (CORRIGIDA)
          dias_desde() {
            if [ -z "$1" ] || [ "$1" = "null" ] || [ "$1" = "" ]; then
              echo "999"
            else
              data_epoch=$(date -d "$1" +%s 2>/dev/null || echo "0")
              hoje_epoch=$(date -d "$HOJE" +%s)
              if [ "$data_epoch" = "0" ]; then
                echo "999"
              else
                echo $(( (hoje_epoch - data_epoch) / 86400 ))
              fi
            fi
          }
          
          # Função para normalizar texto (NOVA)
          normalizar_texto() {
            echo "$1" | tr '[:upper:]' '[:lower:]' | sed 's/^ *//;s/ *$//'
          }
          
          # Verificar se arquivo existe
          if [ ! -f "dados.geojson" ]; then
            echo "❌ Arquivo dados.geojson não encontrado"
            MESSAGE="⚠️ *Sítio Ipiranga - Sem dados*%0A"
            MESSAGE="${MESSAGE}Arquivo dados.geojson não encontrado.%0A"
            MESSAGE="${MESSAGE}Exporte os dados do QField.%0A"
            MESSAGE="${MESSAGE}%0A📅 ${DATA_EXIBICAO} ${HORA_EXIBICAO}"
            
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
              -d chat_id="${TELEGRAM_CHAT_ID}" \
              -d text="${MESSAGE}" \
              -d parse_mode="Markdown"
            
            exit 0
          fi
          
          # Contar total de plantas
          TOTAL=$(jq '.features | length' dados.geojson 2>/dev/null || echo "0")
          
          # Saudação
          if [ "$HORA_NUM" -lt 12 ]; then
            SAUDACAO="☀️ Bom dia"
          elif [ "$HORA_NUM" -lt 18 ]; then
            SAUDACAO="⛅ Boa tarde"
          else
            SAUDACAO="🌙 Boa noite"
          fi
          
          # Iniciar mensagem
          MESSAGE="${SAUDACAO}!%0A"
          MESSAGE="${MESSAGE}🌿 *Sítio Ipiranga*%0A"
          MESSAGE="${MESSAGE}📅 ${DATA_EXIBICAO} às ${HORA_EXIBICAO}%0A"
          MESSAGE="${MESSAGE}🌳 *${TOTAL} plantas* cadastradas%0A%0A"
          
          # Se for push event
          if [ "$GITHUB_EVENT_NAME" = "push" ]; then
            MESSAGE="${MESSAGE}🔄 *Dados atualizados*%0A"
          fi
          
          # Processar ações se tiver plantas
          if [ "$TOTAL" -gt "0" ]; then
            # Arrays para ações
            REGA_URG=""
            REGA_NORM=""
            PODA_PEND=""
            COLHEITA_PEND=""
            TOTAL_ACOES=0
            
            # Processar cada planta
            jq -c '.features[]' dados.geojson 2>/dev/null | while read -r feature; do
              # Extrair em MINÚSCULAS (compatível com PWA)
              ID=$(echo "$feature" | jq -r '.properties.id // .properties.fid // "S/N"')
              NOME=$(echo "$feature" | jq -r '.properties.nome_popular // "Planta"')
              ULT_REGA=$(echo "$feature" | jq -r '.properties.ultima_rega // "null"')
              ULT_PODA=$(echo "$feature" | jq -r '.properties.ultima_poda // "null"')
              ULT_COLH=$(echo "$feature" | jq -r '.properties.ultima_colheita // "null"')
              
              # Normalizar nome
              NOME_NORM=$(normalizar_texto "$NOME")
              
              # Determinar intervalos
              if echo "$NOME_NORM" | grep -q "mangabeira"; then
                REGA_INT=7; PODA_INT=180; COLHEITA_INT=90
              elif echo "$NOME_NORM" | grep -q "caju"; then
                REGA_INT=10; PODA_INT=365; COLHEITA_INT=60
              elif echo "$NOME_NORM" | grep -q "coq"; then
                REGA_INT=5; PODA_INT=730; COLHEITA_INT=30
              elif echo "$NOME_NORM" | grep -q "cacau"; then
                REGA_INT=3; PODA_INT=180; COLHEITA_INT=120
              else
                REGA_INT=7; PODA_INT=180; COLHEITA_INT=90
              fi
              
              # Verificar rega
              DIAS_R=$(dias_desde "$ULT_REGA")
              if [ "$DIAS_R" -ge $((REGA_INT + 2)) ]; then
                REGA_URG="${REGA_URG}• 🚨 ${NOME} #${ID} - ${DIAS_R} dias%0A"
                TOTAL_ACOES=$((TOTAL_ACOES + 1))
              elif [ "$DIAS_R" -ge "$REGA_INT" ]; then
                REGA_NORM="${REGA_NORM}• 💧 ${NOME} #${ID} - ${DIAS_R} dias%0A"
                TOTAL_ACOES=$((TOTAL_ACOES + 1))
              fi
              
              # Verificar poda
              DIAS_P=$(dias_desde "$ULT_PODA")
              if [ "$DIAS_P" -ge "$PODA_INT" ]; then
                PODA_PEND="${PODA_PEND}• ✂️ ${NOME} #${ID} - ${DIAS_P} dias%0A"
                TOTAL_ACOES=$((TOTAL_ACOES + 1))
              fi
              
              # Verificar colheita
              DIAS_C=$(dias_desde "$ULT_COLH")
              if [ "$DIAS_C" -ge "$COLHEITA_INT" ]; then
                COLHEITA_PEND="${COLHEITA_PEND}• 🍎 ${NOME} #${ID} - ${DIAS_C} dias%0A"
                TOTAL_ACOES=$((TOTAL_ACOES + 1))
              fi
            done
            
            # Montar mensagem final
            if [ "$TOTAL_ACOES" -eq 0 ]; then
              MESSAGE="${MESSAGE}✅ *Tudo em dia!*%0ANenhuma ação pendente.%0A"
            else
              MESSAGE="${MESSAGE}📋 *${TOTAL_ACOES} ação(ões) pendente(s):*%0A%0A"
              [ -n "$REGA_URG" ] && MESSAGE="${MESSAGE}🚨 *URGENTE:*%0A${REGA_URG}%0A"
              [ -n "$REGA_NORM" ] && MESSAGE="${MESSAGE}💧 *Rega:*%0A${REGA_NORM}%0A"
              [ -n "$PODA_PEND" ] && MESSAGE="${MESSAGE}✂️ *Poda:*%0A${PODA_PEND}%0A"
              [ -n "$COLHEITA_PEND" ] && MESSAGE="${MESSAGE}🍎 *Colheita:*%0A${COLHEITA_PEND}%0A"
            fi
          else
            MESSAGE="${MESSAGE}⚠️ *Nenhuma planta cadastrada*%0A"
          fi
          
          MESSAGE="${MESSAGE}%0A📱 *Acesse o app:*%0A"
          MESSAGE="${MESSAGE}https://yurimegromonte182.github.io/S-tio_Jintanga/"
          
          # Enviar mensagem
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            -d text="${MESSAGE}" \
            -d parse_mode="Markdown"
          
          echo "✅ Notificação enviada!"
          
      - name: Estatísticas diárias (06h)
        if: github.event.schedule == '0 9 * * *'
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ -f "dados.geojson" ]; then
            TOTAL=$(jq '.features | length' dados.geojson)
            
            # Contar por tipo (case-insensitive)
            COQUEIROS=$(jq '[.features[] | select(.properties.nome_popular // "" | ascii_downcase | contains("coq"))] | length' dados.geojson)
            CAJUEIROS=$(jq '[.features[] | select(.properties.nome_popular // "" | ascii_downcase | contains("caju"))] | length' dados.geojson)
            CACAU=$(jq '[.features[] | select(.properties.nome_popular // "" | ascii_downcase | contains("cacau"))] | length' dados.geojson)
            MANGABEIRA=$(jq '[.features[] | select(.properties.nome_popular // "" | ascii_downcase | contains("mangabeira"))] | length' dados.geojson)
            
            STATS="📊 *Resumo do Sítio:*%0A"
            STATS="${STATS}🌳 Total: ${TOTAL} plantas%0A"
            [ "$COQUEIROS" -gt 0 ] && STATS="${STATS}🥥 Coqueiros: ${COQUEIROS}%0A"
            [ "$CAJUEIROS" -gt 0 ] && STATS="${STATS}🌰 Cajueiros: ${CAJUEIROS}%0A"
            [ "$CACAU" -gt 0 ] && STATS="${STATS}🍫 Cacau: ${CACAU}%0A"
            [ "$MANGABEIRA" -gt 0 ] && STATS="${STATS}🥭 Mangabeira: ${MANGABEIRA}%0A"
            
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
              -d chat_id="${TELEGRAM_CHAT_ID}" \
              -d text="${STATS}" \
              -d parse_mode="Markdown"
          fi
