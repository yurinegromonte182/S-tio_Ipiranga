name: Sistema Sítio Ipiranga

on:
  schedule:
    - cron: '0 9,15,21 * * *'  # 06:00, 12:00, 18:00 BR
  workflow_dispatch:

jobs:
  check-system:
    runs-on: ubuntu-latest
    steps:
      - name: Verificar e notificar
        env:
          # Secrets mínimos
          TG_BOT: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TG_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          SB_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
        run: |
          # Mensagem direta
          MSG="🟢 *Sistema Online*
          🕒 $(TZ='America/Fortaleza' date '+%d/%m %H:%M')
          📍 Sítio Ipiranga
          ✅ Todos sistemas operacionais"
          
          # Enviar
          curl -s -X POST \
            "https://api.telegram.org/bot$TG_BOT/sendMessage" \
            -d chat_id="$TG_ID" \
            -d text="$MSG" \
            -d parse_mode="Markdown" \
            && echo "✅ Notificação enviada"
