name: 🔍 VERIFICAR SECRETS

on:
  workflow_dispatch:

jobs:
  verificar:
    runs-on: ubuntu-latest
    steps:
      - name: Verificar Secrets
        env:
          TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          CHAT: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          echo "=== VERIFICAÇÃO DE SECRETS ==="
          echo ""
          
          echo "1. TELEGRAM_BOT_TOKEN:"
          if [ -n "$TOKEN" ]; then
            echo "   ✅ CONFIGURADO"
            echo "   Primeiros 10 caracteres: ${TOKEN:0:10}..."
            echo "   Comprimento: ${#TOKEN} caracteres"
          else
            echo "   ❌ NÃO CONFIGURADO (vazio)"
          fi
          
          echo ""
          echo "2. TELEGRAM_CHAT_ID:"
          if [ -n "$CHAT" ]; then
            echo "   ✅ CONFIGURADO"
            echo "   Valor: $CHAT"
          else
            echo "   ❌ NÃO CONFIGURADO (vazio)"
          fi
          
          echo ""
          echo "=== CONCLUSÃO ==="
          
          if [ -n "$TOKEN" ] && [ -n "$CHAT" ]; then
            echo "🎉 TUDO CONFIGURADO CORRETAMENTE!"
            echo "O problema pode estar no workflow."
            exit 0
          else
            echo "⚠️ SECRETS FALTANDO!"
            echo "Configure em: Settings → Secrets → Actions"
            echo "Adicione:"
            echo "- TELEGRAM_BOT_TOKEN: 8246537638:AAGHsDcT9EDVrhvRMgC5Kpj6z6yWSJT8ONU"
            echo "- TELEGRAM_CHAT_ID: [seu número]"
            exit 1
          fi
